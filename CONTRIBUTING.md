# Contributing

Thanks for considering contributing!

## Development

- Node.js: 20+
- Install: `npm ci`
- Tests: `npm test`
- Build: `npm run build`

## Guidelines

- Keep API changes backwards-compatible when possible.
- Add tests for bug fixes / new features.
- Keep generated artifacts out of git (`dist/`, `node_modules/`).
